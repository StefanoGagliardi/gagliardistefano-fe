/**
 * General interface for return function.
 * This function is a custom hook for Wrap form handling via react hook form
 */
export interface FormRHooketurn {}

/*
 * Contact form
 */
export interface ContactFormData {
  name: string;
  email: string;
  message: string;
}
